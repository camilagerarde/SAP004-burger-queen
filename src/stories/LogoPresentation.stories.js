import React from "react";
import LogoPresentation from "../components/LogoPresentation";

export default {
  title: "LogoPresentation",
  component: LogoPresentation,
};

export const Default = () => <LogoPresentation />;
